# Optional: enables the AI caption starter. The Unicode converters work without it.
HF_TOKEN=
HF_MODEL=Qwen/Qwen3-0.6B

# Used for canonical metadata and sitemap URLs.
NEXT_PUBLIC_SITE_URL=https://smalltextgen.site

# Optional public support link.
NEXT_PUBLIC_CONTACT_EMAIL=
